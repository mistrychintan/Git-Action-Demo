name: workflow-1
on: workflow_dispatch

jobs:
  job-1:
    runs-on: ubuntu-22.04
    steps:
      - name: print message
      run: echo "hello world to git action"

      - name: print second message
      run: echo "welcome to my page to see git action"
      
